# ts-simple-starter

## Setup

```
yarn
```

## Run

```bash
yarn run index.ts
```

### `run`コマンドの増やし方

`package.json`の`scripts`に

```bash
"hoge.ts": "ts-node -P tsconfig.json ./src/hoge.ts"
```

のように追加します。
